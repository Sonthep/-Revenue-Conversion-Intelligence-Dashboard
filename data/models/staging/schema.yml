version: 2

sources:
  - name: app
    description: "Raw application data"
    tables:
      - name: orders
        columns:
          - name: order_id
            tests:
              - not_null
              - unique
          - name: account_id
            tests:
              - not_null
          - name: user_id
            tests:
              - not_null
          - name: product_id
            tests:
              - not_null
          - name: order_ts
            tests:
              - not_null
          - name: gross_amount
            tests:
              - not_null
      - name: accounts
      - name: users
      - name: products
