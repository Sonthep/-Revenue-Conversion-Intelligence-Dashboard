name: "revenue_dashboard"
version: "1.0"
config-version: 2

profile: "revenue_dashboard"

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets: ["target", "dbt_packages"]

models:
  revenue_dashboard:
    staging:
      +materialized: view
    marts:
      +materialized: table
